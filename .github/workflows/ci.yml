name: ci

on: [push]

jobs:
  test:
    strategy:
      matrix:
        module:
          - cdn
          - certificate
          - cognito
          - ecr
          - ecs
          - kms-key
          - nat
          - rds
          - secrets
          - vpc
      fail-fast: false
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    steps:
      - uses: actions/checkout@v2

      - name: init
        run: |
          cd ${{ matrix.module }}
          terraform init

      - name: validate
        run: |
          cd ${{ matrix.module }}
          terraform validate

      - name: fmt -check
        run: |
          cd ${{ matrix.module }}
          terraform fmt -check
